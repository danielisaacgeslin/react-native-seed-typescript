import { StyleSheet } from 'react-native';

import { STYLE } from '../../../../../constants';

export default StyleSheet.create({
  avatar: {
    borderRadius: STYLE.BORDER.GLOBAL_RADIUS,
    height: 36,
    marginRight: 10,
    width: 36
  }
});
